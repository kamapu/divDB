

update_geom <- function(conn, geom, idx, tmp = "_tmp", ...) {
  # Check existence of geometry
  # Check existence of idx
  # Check non existence of temporary table
  # Do temporary table
  # Update from temporary table
}
